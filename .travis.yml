sudo: false

language: ruby

rvm:
  - 2.4
  - 2.5
  - 2.6
  - 2.7
  - ruby-head

before_install: gem install bundler

addons:
  postgresql: "9.6"

services:
  - postgresql

matrix:
  allow_failures:
    - rvm: ruby-head
